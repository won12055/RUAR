# Dependencies for full RUAR training.
#
# Install a CUDA-compatible PyTorch build before this file. The experiments were
# validated with torch 2.10.0, vLLM 0.17.1, transformers 4.57.6, and Python 3.10.
# Example PyTorch command depends on your CUDA driver/toolkit, so it is kept in
# docs/installation.md rather than pinned here.
#
# Paper-default training uses USE_REMOVE_PADDING=True. flash-attn is not pinned
# here because its wheel/source build must match your Python, PyTorch, and CUDA
# stack. Install a compatible flash-attn build after PyTorch, verify that
# `import flash_attn, flash_attn_2_cuda` succeeds, or set
# USE_REMOVE_PADDING=False before launching a non-paper-default smoke run.

accelerate==1.13.0
codetiming==1.4.0
datasets==4.8.5
dill==0.4.1
hydra-core==1.3.2
liger-kernel==0.8.0
numpy==2.2.6
omegaconf==2.3.0
pandas==2.3.3
pyarrow==24.0.0
pylatexenc==2.10
sympy==1.14.0
ray[data,train,tune,serve]==2.55.1
tensordict==0.12.2
tokenizers==0.22.2
tqdm==4.67.3
transformers==4.57.6
vllm==0.17.1
wandb==0.26.1
